
    type User {
      id: ID!
      email: String
      password: String
    }
    
    input AuthData {
      email: String!
      password: String!
    }

    type Mutation {
      createUser(authProvider: AuthData!): User
      createLink(url: String!, description: String!): Link
    }
